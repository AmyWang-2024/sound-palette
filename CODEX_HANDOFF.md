# Sound Palette — 产品需求与 Codex 工程交接主文档

- **项目名称：** Sound Palette / 声音调色盘
- **当前产品名称：** 我的声音相册（微信小程序）/ Sound Palette
- **版本范围：** Web MVP v0.1、微信小程序 v0.1、已批准的后续 AI 路线
- **首次交接日期：** 2026-07-24
- **本次规格更新：** 2026-07-27
- **开发环境：** Windows + GitHub + Codex
- **当前分支：** `feat/sound-palette-ai-v0.1`
- **固定执行顺序：** Web M0–M5 → W0–W5 → AI-M0–AI-M5（仓库实现已完成，外部门禁未关闭）
- **优先级：** 先完成当前可体验闭环；节省 Token；避免过度工程化。

## 0. 文档地位、状态与优先级

### 0.1 唯一主文档

本文件是项目需求、隐私边界、工程约束、里程碑和发布门禁的唯一规范性主文档。
其他 README、测试手册、隐私草案、研究材料和历史交接文件只能补充证据或操作步骤；
如与本文件冲突，以本文件为准，并应修正冲突文件。

平台专属规则只覆盖其明确标注的平台和阶段：

- 第 1–17 节是已经完成的 Web MVP 基线；
- 第 18 节是当前微信原生小程序 v0.1；
- 第 19 节是 W5 之后才可实施的 AI 延伸路线；
- 第 20 节是 Codex 的统一执行规则。

后出现的阶段不得反向改变已发布版本的事实。例如，未来 AI 模式允许在明确授权后临时
上传 PCM，不代表 Web 或小程序本地模式曾经上传声音。

### 0.2 当前仓库事实

| 项目 | 当前状态 | 证据或门禁 |
|---|---|---|
| Web M0–M5 | 已完成基线 | Web 功能分支已有独立里程碑提交 |
| W0 | 已提交 | `ebb3065` |
| W1 | 已提交 | `45b0df7` |
| W2 | 实现与自动检查完成，真机验收待完成 | 独立 W2 实现提交与 `docs/miniprogram/W2_MANUAL_TEST.md` |
| Android W2 | 需要对最新修复重新完整验证 | 旧结果和修复前截图不能替代最新构建验收 |
| iPhone W2 | 未验证 | 暂无可用设备，是 W2 与发布门禁 |
| W3 | 实现与自动检查完成，真机验收待完成 | 独立 W3 实现提交与 `docs/miniprogram/W3_MANUAL_TEST.md` |
| W4 | 实现与开发者工具检查完成，真机验收待完成 | W4 QA、设备记录和审核材料 |
| W5 | 仓库发布准备完成，外部门禁未关闭 | `RELEASE_EVIDENCE.md` 与平台凭证 |
| AI-M0 | 实现与自动检查完成，默认关闭 | 纯本地 mock、错误码和幂等测试 |
| AI-M1 | 实现与自动检查完成 | 20 段本地声纹、稳定 seed、八套配方及 fixture |
| AI-M2 | 本地 mock 服务与自动检查完成，默认关闭 | 最小账户、额度流水、任务、预算、恢复和幂等测试 |
| AI-M3 | 安全模拟链路与自动检查完成，真实门禁未关闭 | Caption 过滤、内存 PCM、删除证据、禁用的 Qwen 候选适配器 |
| AI-M4 | 本地框架与自动检查完成，真实门禁未关闭 | 确定性提示词、候选图像契约、混合 Canvas、标识门禁和盲测计划 |
| AI-M5 | 仓库门禁、运维控制和审核材料完成；外部阻塞 | `check:ai-release` 返回 `blocked-external-gates`，AI 保持关闭 |

里程碑状态必须以 Git 提交、当前工作区、自动测试和真机记录共同判断，不能只依据本表。
每次完成里程碑后更新本表和 `FINAL_REPORT.md`；不得把“已编写代码”等同于“已通过真机验收”。

### 0.3 规则优先级

发生冲突时依次采用：

1. 用户在当前任务中的明确指令；
2. 本文件中当前平台和当前里程碑的具体规则；
3. 本文件的跨平台共同约束；
4. 历史阶段规则和建议性文案；
5. 其他非规范性文档。

---

# 第一部分：Web MVP v0.1 已完成基线

本部分记录 Web 版的既有产品行为、技术栈和验收事实。除修复 Web 回归外，它不是当前实施
范围；其中“首版”“M0–M5”和浏览器相关禁令均只指 Web MVP。

## 1. Web 项目目标

开发一个无需下载安装、无需登录、无需服务器的手机网页原型。

用户完成一次体验的流程：

1. 打开网页；
2. 点击“开始聆听”；
3. 网页在本机分析 10 秒环境声音；
4. 用户选择 Mood：**好 / 不好不坏 / 不好**；
5. 系统根据声音特征与 Mood 生成一幅轻微动态的抽象声音画；
6. 显示三层声景构成与三个标签；
7. 用户可以换一种 Mood、再听一次、保存 PNG 或调用系统分享。

核心验证命题：

> 用户是否愿意把“周围的声音 + 当下的心情”转化为一幅作品，并保存或分享。

---

## 2. Web MVP 必须坚持的范围

### 2.1 必须完成

- 手机麦克风授权与异常处理；
- 10 秒实时聆听；
- 相对响度、低频、中频、高频、变化程度的本地分析；
- 一种稳定的抽象画风格；
- 三种 Mood；
- 同一段声音切换 Mood 时保持同一构图骨架；
- 三层声音构成；
- 三个规则标签；
- 无麦克风权限时的示例模式；
- PNG 结果卡片导出；
- Web Share API 分享，失败时提供保存图片兜底；
- iPhone/iPad Safari 与 Android Chrome 的基本适配；
- 静态生产构建。

### 2.2 Web M0–M5 明确不做

- 不识别汽车、风、鸟鸣、人声等具体声源；
- 不显示未经校准的绝对分贝值；
- Web 不录制、保存或上传原始声音；
- 不做语音转文字；
- 不做账号、登录、数据库、云端历史；
- 不做点赞、评论、关注或社区；
- 不做地图和定位；
- Web M0–M5 不包含微信小程序；小程序要求见第 18 节；
- 不做原生 iOS 或 Windows App；
- 不做多种画风；
- 不复制或依赖 AURA 源代码。

任何不属于“必须完成”的功能，不得自行加入。

AI 声音理解、图像生成与生成式文案不属于 Web M0–M5。已批准的后续路线见第 19 节，
但不得改变 Web 版“不上传原始声音”的隐私事实。

---

## 3. 技术栈

采用成熟且许可证宽松的现有技术，不重新发明底层通用能力。

### 3.1 核心技术

| 技术 | 用途 | 备注 |
|---|---|---|
| Vite | 开发服务器与生产构建 | 使用 Vanilla TypeScript 模板 |
| TypeScript | 类型约束与模块组织 | 不使用 React |
| Web Audio API | 麦克风、波形、FFT | 浏览器原生能力，不安装音频框架 |
| PixiJS | 2D GPU 动态绘制 | 主视觉引擎 |
| simplex-noise | 连续、有机的流动噪声 | 生成柔和形变和运动场 |
| seedrandom | 可重复的构图随机数 | 同一声音切换 Mood 时保持构图 |
| Culori | 色彩空间、插值与 Mood 调色 | 不手写大量零散十六进制色值 |
| Vitest | 核心规则单元测试 | 只测试纯函数和数据规则 |

### 3.2 暂不安装

- React；
- Three.js；
- Tone.js；
- Meyda；
- p5.js；
- TensorFlow.js；
- Essentia.js；
- 任意后端框架；
- 任意数据库；
- 任意状态管理框架。

只有在现有技术无法满足已经确定的验收标准时，才允许增加依赖，并在提交说明中解释原因。
本清单只约束 Web M0–M5 的锁定技术栈，不构成对第 19 节未来 AI 里程碑必要依赖的禁止。

### 3.3 初始化命令

在空目录中执行；若仓库已有文件，不得覆盖，先检查现状。

```powershell
npm create vite@latest . -- --template vanilla-ts
npm install pixi.js simplex-noise seedrandom culori
npm install -D @types/seedrandom vitest
```

规则：

- 只安装正式稳定版，不使用 `next`、`beta`、Git commit 或 CDN 版本；
- 生成并提交 `package-lock.json`；
- 后续以 lockfile 为准，不无故升级；
- 不运行会自动升级大版本的 `npm audit fix --force`；
- Vite 当前要求 Node.js 20.19+ 或 22.12+。若环境不满足，停止并报告，不擅自改装系统。

---

## 4. 产品页面与用户流程

MVP 只需要四个主状态，不必引入路由框架。

### 4.1 首页 `home`

文案：

- 标题：`Sound Palette`
- 中文名：`声音调色盘`
- 主文案：`听见的，也可以被看见。`
- 主按钮：`开始聆听`
- 次按钮：`先体验示例`
- 隐私提示：`声音只在你的设备上实时分析，不录音、不上传。`

点击主按钮后才请求麦克风权限，不得在页面加载时请求。

### 4.2 聆听页 `listening`

- 倒计时 10 秒；
- 动态画面从第一秒开始响应；
- 显示“正在聆听”，不显示复杂频谱仪；
- 提供“停止”按钮；
- 页面进入后台或失去可见性时暂停或安全结束；
- Web 录制期间不创建 `MediaRecorder`，不生成音频文件。小程序的本地临时文件规则见第 18.3 节。

### 4.3 Mood 页 `mood`

问题：`此刻，你感觉怎么样？`

三个选项：

- `好`
- `不好不坏`
- `不好`

不要使用夸张笑脸或哭脸。使用简洁的文字与抽象色块，同时保证文字标签清晰，不能只靠颜色表达。

### 4.4 结果页 `result`

必须显示：

- 动态抽象作品；
- Mood；
- 三层声景构成；
- 三个标签；
- `换一种 Mood`；
- `再听一次`；
- `保存图片`；
- `分享`。

---

## 5. 音频分析方案

### 5.1 数据流

```text
getUserMedia({ audio: true })
  → AudioContext
  → MediaStreamAudioSourceNode
  → AnalyserNode
  → 时域数据 + 频域数据
  → 实时 AudioFrame
  → 10 秒 SoundSummary
```

### 5.2 `AnalyserNode` 初始参数

建议初值：

```ts
fftSize = 2048
smoothingTimeConstant = 0.75
minDecibels = -90
maxDecibels = -10
```

这些只是初值，可根据真实设备测试调整。调整必须记录原因。

### 5.3 数据类型

```ts
export interface AudioFrame {
  timestampMs: number;
  loudness: number;    // 0..1，相对值
  lowEnergy: number;   // 0..1
  midEnergy: number;   // 0..1
  highEnergy: number;  // 0..1
  changeRate: number;  // 0..1
}

export interface SoundSummary {
  durationMs: number;
  loudnessMean: number;
  loudnessPeak: number;
  lowEnergy: number;
  midEnergy: number;
  highEnergy: number;
  changeRate: number;
  quiet: boolean;
  composition: {
    base: number;
    flow: number;
    sparkle: number;
  };
  seed: string;
}
```

所有数值在进入视觉引擎前必须进行有限值检查与 0..1 限制，不能让 `NaN` 或 `Infinity` 进入绘制循环。

### 5.4 频段划分

初版：

- 低频：40–250 Hz；
- 中频：250–2,000 Hz；
- 高频：2,000–8,000 Hz；
- 高频上限不得超过 Nyquist 频率。

根据 `AudioContext.sampleRate` 和 FFT bin 数自动计算 bin 范围，禁止写死 bin 索引。

### 5.5 相对响度

从时域数组计算 RMS，再映射到 0..1。

重要：

- 这是相对响度，不是经过校准的 dB SPL；
- UI 不得显示“63 dB”之类的绝对声压值；
- 可以显示 `声音很轻 / 适中 / 较强`，但首版结果页不强制显示响度文字。

### 5.6 变化程度

基于连续帧的以下变化计算，并使用 EMA 平滑：

- loudness 差值；
- 三个频段能量差值；
- 突发峰值数量。

无需复杂音乐节拍检测。

### 5.7 十秒摘要

- 对所有有效帧取均值或稳健统计；
- 峰值单独保留；
- 低中高三段能量归一化为三层构成；
- 三层名称：
  - `基底声` = base = 低频；
  - `流动声` = flow = 中频；
  - `闪烁声` = sparkle = 高频。

如果有效总能量低于安静阈值：

- `quiet = true`；
- 不伪造 33/33/34；
- 三层构成可以全部为 0；
- 画面以留白、轻微呼吸和低密度纹理呈现；
- 标签使用“安静”“留白”等规则。

### 5.8 清理资源

每次结束或取消时必须：

- 停止 `MediaStreamTrack`；
- 断开 AudioNode；
- 取消动画帧；
- 释放或关闭不再使用的 AudioContext；
- 页面刷新后不得保存原始声音或音频缓冲。

---

## 6. 视觉引擎

### 6.1 唯一画风

首版只做：

> 流体色场 + 柔和曲线/色带 + 少量粒子。

不要做：

- 普通频谱柱；
- 三维地形；
- 数字雨；
- 赛博朋克霓虹；
- 多套主题选择；
- 复杂 Shader 编辑器。

### 6.2 PixiJS 图层

建议使用四层：

1. `BackgroundLayer`：背景渐变与缓慢色场；
2. `BaseLayer`：2–4 个大尺度、缓慢形变的低频形态；
3. `FlowLayer`：中频驱动的曲线、丝带或波纹；
4. `SparkleLayer`：高频驱动的少量粒子和短线。

初版可以都写在一个 `ArtEngine` 中，只有当文件明显过大时才拆层。不要为了架构美观提前拆出大量空模块。

### 6.3 声音到画面的映射

| 声音特征 | 视觉参数 |
|---|---|
| loudness | 整体尺度、呼吸幅度、透明度上限 |
| lowEnergy | 大色块面积、厚重度、慢速位移 |
| midEnergy | 色带宽度、曲率、流动速度 |
| highEnergy | 粒子数量、亮点尺寸、闪烁频率 |
| changeRate | 方向变化、扰动强度、运动活跃度 |
| quiet | 留白、低密度、极慢运动 |

### 6.4 固定构图

一次 10 秒会话结束时只生成一次 `seed`。

- `seedrandom` 必须创建局部 PRNG；
- 不得替换全局 `Math.random()`；
- 用户切换 Mood 时，seed、形态数量、初始位置和基本构图不变；
- Mood 只能改变色彩、明暗、速度、密度、扩张/收缩和运动倾向。

### 6.5 性能约束

- 移动端目标不低于约 30 FPS；
- `devicePixelRatio` 上限设为 2；
- 初始粒子数不超过 160；
- 不在每一帧创建大量新对象；
- 复用 Graphics、数组和粒子对象；
- 页面不可见时暂停 ticker；
- 监听 resize 与 orientation change；
- 对低性能设备自动降低粒子数量，而不是崩溃。

---

## 7. Mood Engine

### 7.1 原则

> Mood 给声音上色，但不篡改声音。

Mood 不得修改：

- 声音摘要；
- 三层比例；
- seed；
- 基础构图。

### 7.2 数据结构

```ts
export type Mood = 'good' | 'neutral' | 'low';

export interface MoodProfile {
  id: Mood;
  labelZh: string;
  brightness: number;
  chroma: number;
  warmth: number;
  expansion: number;
  motionSpeed: number;
  textureDensity: number;
  edgeSoftness: number;
  drift: 'outward' | 'balanced' | 'inward';
}
```

### 7.3 初始参数方向

参数可微调，但必须保留明显差异：

#### `good / 好`

- 更亮；
- 色彩稍丰富；
- 略偏暖；
- 形态舒展；
- 运动稍快但不躁；
- 纹理较疏；
- 漂移倾向向外或向上。

#### `neutral / 不好不坏`

- 中等亮度；
- 中性色温；
- 构图平衡；
- 运动稳定；
- 留白适中；
- 不做戏剧化处理。

#### `low / 不好`

- 降低亮度，但不能简单变黑；
- 略偏冷或偏钝；
- 构图收缩；
- 纹理稍密；
- 运动迟缓或有阻滞；
- 漂移倾向向内或向下。

### 7.4 色彩

使用 Culori 在感知更均匀的色彩空间中完成插值、明度和色度调整。

不要：

- 把“好”固定成黄色；
- 把“不好”固定成蓝色；
- 为每个状态复制三套完全无关的画面代码。

---

## 8. 标签规则

现有 M0–M5 的标签继续使用稳定规则生成；后续 AI 里程碑可以增加生成式作品或文案，
但不得在未更新验收标准时破坏这三个标签的稳定性。每幅作品固定生成三个标签：

1. 一个结构标签；
2. 一个运动标签；
3. 一个 Mood 标签。

### 8.1 结构标签

- quiet：`安静` 或 `留白`；
- base 明显占优：`厚重基底`；
- flow 明显占优：`持续流动`；
- sparkle 明显占优：`明亮细节`；
- 三者接近：`层次均衡`。

### 8.2 运动标签

- changeRate 低：`缓慢`；
- changeRate 中：`流动`；
- changeRate 高：`跳跃` 或 `起伏明显`。

### 8.3 Mood 标签

- good：`舒展` / `清亮` / `有生气` 中选一；
- neutral：`日常` / `平稳` / `观察中` 中选一；
- low：`下沉` / `收拢` / `沉静` 中选一。

同一 seed 下标签选择也要稳定，不能每次重绘随机变化。

---

## 9. 示例模式

不使用任何下载的录音、音乐或版权不明素材。

示例模式直接提供合成的声音特征数据，并让画面按预设曲线变化。

建议三个预设：

```ts
const samples = {
  parkMorning: {
    low: 0.20,
    mid: 0.35,
    high: 0.45,
    loudness: 0.35,
    changeRate: 0.45,
  },
  rainyStreet: {
    low: 0.45,
    mid: 0.45,
    high: 0.10,
    loudness: 0.60,
    changeRate: 0.28,
  },
  cafeAfternoon: {
    low: 0.25,
    mid: 0.60,
    high: 0.15,
    loudness: 0.45,
    changeRate: 0.22,
  },
};
```

首页“先体验示例”可先随机进入其中一个；结果页可注明“示例声景”。

---

## 10. 导出与分享

### 10.1 导出卡片

导出一张竖版 PNG，目标尺寸：

```text
1080 × 1440
```

卡片内容：

- `SOUND PALETTE`；
- 日期和时间；
- 静态抽象作品；
- Mood；
- 基底声 / 流动声 / 闪烁声；
- 三个标签；
- 小型品牌标识。

绘制方式：

- 先从 PixiJS 获取作品快照；
- 再使用离屏 Canvas 2D 合成文字和版式；
- 输出 PNG Blob；
- 不依赖 DOM 截图库。

PixiJS 的提取 API可能随版本变化，按项目实际安装版本的官方 API 实现，不要照抄旧版示例。

### 10.2 分享逻辑

优先级：

1. 浏览器支持 `navigator.share` 且支持文件分享：分享 PNG；
2. 否则下载或打开 PNG；
3. 若 iOS Safari 不允许自动下载，给出明确提示：长按图片保存或调用系统分享。

分享失败不得导致作品丢失。

---

## 11. Web 隐私与安全

### 11.1 Web 强制要求

- 不使用 `MediaRecorder`；
- 不生成音频 Blob；
- Web 不上传麦克风数据；
- 不把原始时域或频域帧写入本地存储；
- 不做语音识别；
- 不收集定位；
- 不接入统计 SDK；
- 不加载第三方广告或追踪代码。

只允许在内存中保留：

- 10 秒期间的数值特征；
- 会话结束后的 `SoundSummary`；
- 当前 Mood；
- 视觉 seed；
- 导出图片。

### 11.2 权限异常

必须处理：

- 用户拒绝；
- 浏览器不支持；
- 非 HTTPS；
- 没有麦克风设备；
- 权限被系统关闭；
- 微信内置浏览器无法工作；
- AudioContext 处于 suspended；
- 页面切后台。

统一提供示例模式兜底，并提示：

> 当前浏览器无法使用麦克风，可以先体验示例；也可以在 Safari 或 Chrome 中重新打开。

### 11.3 无障碍

- 所有按钮提供可读文字和可访问名称；
- Mood 不能只用颜色区分；
- 文本与背景对比度足够；
- 支持键盘基本操作；
- 尊重 `prefers-reduced-motion`，降低动画速度和粒子数量；
- Canvas 旁提供文字版结果摘要。

---

## 12. 建议目录结构

保持小而清晰，不要提前做大型架构。

```text
sound-palette/
├── public/
│   └── icons/
├── src/
│   ├── main.ts
│   ├── styles.css
│   ├── app-controller.ts
│   ├── types.ts
│   ├── audio-engine.ts
│   ├── sound-summary.ts
│   ├── art-engine.ts
│   ├── mood-profiles.ts
│   ├── tag-engine.ts
│   ├── sample-scenes.ts
│   └── export-card.ts
├── tests/
│   ├── sound-summary.test.ts
│   ├── mood-profiles.test.ts
│   └── tag-engine.test.ts
├── index.html
├── package.json
├── package-lock.json
├── tsconfig.json
├── README.md
├── PRIVACY.md
├── THIRD_PARTY_NOTICES.md
├── AGENTS.md
└── CODEX_HANDOFF.md
```

如果某文件超过约 400–500 行且职责明显混杂，再拆分；不要为了满足行数而机械拆分。

---

## 13. Web M0–M5 开发里程碑

每个里程碑必须：

- 本地可运行；
- 通过相关测试；
- `npm run build` 成功；
- 提交一次清晰的 Git commit；
- 再进入下一里程碑。

### M0：项目骨架

目标：建立可运行的 Vite + TypeScript + PixiJS 项目。

完成：

- 初始化项目；
- 安装依赖；
- 建立基本页面状态；
- 创建空的 PixiJS 画布；
- 配置 Vitest；
- 添加 README、PRIVACY、THIRD_PARTY_NOTICES 和 AGENTS。

验收：

```powershell
npm run dev
npm run test
npm run build
```

均成功。

建议 commit：

```text
chore: scaffold Sound Palette web MVP
```

### M1：视觉沙盒，不接麦克风

目标：先验证声音参数与 Mood 的视觉语言。

临时提供开发控制面板：

- loudness；
- low；
- mid；
- high；
- changeRate；
- 三种 Mood。

完成一种流体色场视觉。

验收：

- 参数变化明显影响画面；
- 同一参数切换 Mood 时构图不跳变；
- 三种 Mood 不只是换背景色；
- 移动端和桌面浏览器不报错。

建议 commit：

```text
feat: add seeded sound-driven visual sandbox
```

### M2：麦克风与音频摘要

目标：接入 Web Audio API。

完成：

- 权限流程；
- 实时 AudioFrame；
- 低中高频计算；
- 相对响度；
- 变化程度；
- 10 秒 SoundSummary；
- 静音处理；
- 资源清理；
- 单元测试。

验收：

- 安静、说话、低沉声音、拍手能够产生明显不同数据；
- 拒绝权限时进入示例模式；
- 不生成音频文件；
- 多次重新聆听不重复占用麦克风。

建议 commit：

```text
feat: add local microphone analysis and sound summary
```

### M3：完整产品流程

目标：完成四个主状态。

完成：

- 首页；
- 10 秒聆听；
- Mood 选择；
- 结果页；
- 换 Mood；
- 再听一次；
- 示例模式；
- 标签和三层比例。

验收：

- 无人指导可完成完整流程；
- 切换 Mood 保留同一构图；
- 所有异常都有可理解提示。

建议 commit：

```text
feat: complete Sound Palette MVP interaction flow
```

### M4：导出、分享和移动端 QA

完成：

- 1080×1440 PNG；
- Web Share；
- 下载/保存兜底；
- iOS Safari 与 Android Chrome 样式适配；
- 横竖屏处理；
- reduced motion；
- 性能优化；
- 生产构建。

验收：

- 导出的卡片文字完整、无裁切；
- 分享失败仍可保存；
- 移动端平均视觉体验可接受；
- 页面切后台不会继续高负载运行。

建议 commit：

```text
feat: add artwork export sharing and mobile polish
```

### M5：部署准备

完成：

- `npm run build` 生成 `dist/`；
- 创建 `DEPLOY.md`；
- 写明 EdgeOne Pages 的 GitHub 部署或本地目录部署方式；
- 不在没有用户凭证时尝试登录或部署；
- 检查所有生产资源均为本地依赖，无未知 CDN。

建议 commit：

```text
chore: prepare static deployment documentation
```

---

## 14. 测试要求

### 14.1 单元测试

至少覆盖：

- 频段区间与 bin 换算；
- 三层比例正常归一化；
- 静音时不出现除零或假比例；
- 输入含 `NaN` 时安全降级；
- 同一 seed 生成同一布局参数；
- Mood 不修改 SoundSummary；
- 标签规则稳定；
- 数值被限制在合法范围。

### 14.2 手动测试矩阵

| 环境 | 必测内容 |
|---|---|
| Windows Chrome | 开发与基础流程 |
| Android Chrome | 麦克风、导出、性能 |
| iPad Safari | 权限、横竖屏、分享 |
| iPhone Safari | 权限、导出、后台恢复 |
| 微信内置浏览器 | 检测与外部浏览器提示 |

声音场景：

- 安静房间；
- 正常说话；
- 低沉持续声；
- 拍手或短促高频声；
- 环境背景声；
- 拒绝权限。

---

## 15. MVP 完成标准

只有全部满足，才算 v0.1 完成：

- 手机可打开；
- 用户点击后才请求麦克风；
- 10 秒内画面确实响应声音；
- 安静、说话、拍手的结果明显不同；
- 三个 Mood 的感觉明显不同；
- 切换 Mood 时仍然是同一幅作品；
- 显示基底声、流动声、闪烁声；
- 显示三个稳定标签；
- 可以完成示例体验；
- 可以导出完整 PNG；
- 分享有兜底；
- 不保存、不上传原始声音；
- `npm run test` 通过；
- `npm run build` 通过；
- 无高严重度、与生产依赖直接相关且未说明的安全问题；
- README 能让其他人在 Windows 上启动项目。

---

## 16. 跨阶段 Token 与工程纪律

本项目必须以节省 Codex Token 和减少返工为优先。

### 16.1 Codex 工作规则

1. 每次只处理当前里程碑；
2. 初次阅读后，不反复扫描整个仓库；
3. 不启动 subagent；
4. 不生成长篇过程报告；
5. 不主动重构与当前任务无关的文件；
6. 不整项目重写；
7. 优先复用已经运行的代码；
8. 不自行添加依赖；
9. 不加入用户没有要求的功能；
10. 只运行与当前修改有关的检查，再运行一次总 build；
11. 不重复询问本文件已经明确的决策；
12. 遇到阻塞时，先做最小可行降级，不扩大范围；
13. 每个里程碑结束后提交 Git；
14. 最终汇报只包含：
    - 修改了哪些文件；
    - 已完成什么；
    - 如何测试；
    - 尚存什么问题。

### 16.2 禁止事项

- 禁止为了“更现代”改成 React；
- 禁止为了视觉效果引入 Three.js；
- AI 声音理解、图像生成或生成式文案只能按第 19.24 节的独立里程碑实施；
- 只有获批的 AI 方案确有需要时才允许建立最小后端与额度账户；不得借此扩展社区等无关功能；
- 除第 18.3 节微信运行时临时文件和第 19.3–19.4 节未来 AI 临时 PCM 外，
  禁止把原始音频写入持久文件或历史；
- 禁止使用版权不明的示例音频、字体、图片和图标；
- 禁止复制 AURA 代码；
- 禁止未经说明的大版本依赖升级。

---

## 17. Git 与许可证

### 17.1 Git 工作流

- Web 历史分支：`feat/sound-palette-mvp-v0.1`；
- 本地版小程序分支：`feat/sound-palette-wechat-miniprogram-v0.1`；
- 当前 AI 开发分支：`feat/sound-palette-ai-v0.1`；
- 不得为了继续小程序工作切回 Web 分支并覆盖当前工作区；
- 每个里程碑一个 commit；
- 不提交 `node_modules`、临时录音、构建缓存；
- `dist/` 是否提交由仓库现有策略决定，默认不提交；
- 不 force push 用户主分支；
- 用户未要求时，不自动发布公开仓库。

### 17.2 第三方许可证

主要依赖为 MIT 或 Apache-2.0 类宽松许可证，可用于商业项目，但必须保留相应许可信息。

创建 `THIRD_PARTY_NOTICES.md`，记录：

- 包名；
- 锁定版本；
- 官方仓库；
- 许可证；
- 用途。

不要在项目根目录自动添加 MIT License 给用户自有代码，除非用户明确决定将 Sound Palette 本身以 MIT 开源。

---

## 18. 微信原生小程序 v0.1

本节是当前实施阶段。小程序保留 Web 版，不使用 WebView、React、Taro、服务器或数据库；
只复用纯 TypeScript 核心规则，微信平台能力和 Canvas 2D 由原生小程序代码适配。

### 18.1 已确认的产品与账号事实

- 中国大陆个人主体，账号已注册并已取得 AppID；
- 中文名称：`我的声音相册`；英文品牌继续使用 `Sound Palette`；
- 用户已在平台配置的工具类目包括：综合工具、备忘、图片处理、办公；
- 图标与小程序简介已准备；
- 真实 AppID、AppSecret、管理员凭证和后续供应商密钥不得提交 Git；
- 受版本控制的 `project.config.json` 保持 `touristappid`，真实 AppID 只允许存在于
  Git 忽略的本机 `project.private.config.json`；
- 类目、备案、隐私指引、体验版、审核和发布状态必须以公众平台实际凭证为准；
- 在管理员完成发布前，不得声称小程序已上线。

### 18.2 当前有效的工程结构与类型

- `packages/core`：Web 与小程序共享的纯 TypeScript 逻辑，不引用 DOM、Web Audio、
  微信 API 或 PixiJS；
- `src`：Web 平台适配及兼容 re-export；
- `miniprogram`：原生 WXML、WXSS、TypeScript 页面与微信适配层；
- `project.config.json`：可提交的微信开发者工具项目配置，不含真实凭证。

当前 W2 可以实现和使用的共享契约只有：

- `AudioFrame`、`SoundSummary`、`Mood`、`SoundVisualInput`；
- PCM/频域数值计算与十秒摘要；
- 固定 seed 布局、Mood 更新、稳定标签和示例数据。

第 19 节的时间声纹、八套配方、AI 状态、账户、额度、服务端 API 和模型数据类型都是
未来阶段契约。除非其对应 `AI-Mx` 已开始，否则不得因为类型已经写在规格中而提前实现。

### 18.3 当前小程序隐私与音频生命周期

- 只有用户点击“开始十秒聆听”后才请求隐私授权和 `scope.record`；
- 只分析十秒实时 PCM 数值帧，不识别或转写语音；
- 当前本地模式不上传麦克风帧、声音摘要、Mood、seed、临时音频或作品；
- 微信 `RecorderManager` 可能生成本地临时录音文件。这不是产品保存行为，但程序必须在
  正常结束、提前停止、切后台、中断和异常路径中停止录音、释放监听器并删除临时文件；
- 删除允许在微信仍短暂占用文件时进行有界重试；只有确认文件已不存在才能标记清理成功；
- 删除失败时必须丢弃本次结果、显示脱敏错误并阻止 W2 通过，不得把失败写成成功；
- 分析窗口、帧数组和摘要只驻留当前内存，不写入持久化存储；
- 用户主动保存作品时才允许在 W3 请求相册写入权限；
- 当前隐私指引必须只描述已经实现的本地行为，不得提前描述第 19 节 AI 上传。

三种音频生命周期必须始终区分：

| 阶段 | 音频行为 |
|---|---|
| Web M0–M5 | Web Audio 内存分析，不使用 `MediaRecorder`，不生成录音文件 |
| 小程序 W0–W5 | 微信可能生成本地临时文件，停止后立即删除，不上传 |
| AI-M3 以后 | 只有明确点击 AI 生成后才临时上传 PCM，并按第 19.4 节主动删除和 TTL 兜底 |

### 18.4 W0–W5 里程碑与验收

#### W0：账号与工程骨架（已提交）

- 完成账号、名称、AppID、本机私有配置和个人主体类目准备；
- 建立 `packages/core`、原生 TypeScript 小程序骨架和平台构建；
- Web 使用共享 core，回归测试与生产构建通过；
- 提交：`ebb3065`。

#### W1：原生示例流程（已提交）

- 完成首页、十秒示例、Mood、结果四状态；
- 使用原生 Canvas 2D 绘制固定构图和三种 Mood；
- 不请求麦克风、相册或网络权限；
- 提交：`45b0df7`。

#### W2：麦克风与隐私（实现完成，真机门禁待验）

- 使用 `RecorderManager`、16 kHz 单声道 PCM、2048 点分析窗口和 `fft.js@4.0.4`；
- 完成隐私授权、录音权限拒绝、十秒摘要、提前停止、后台停止、中断和重复聆听；
- 正常和异常路径都释放监听器，并按第 18.3 节确认临时文件删除；
- Android 与 iPhone 分别确认实时帧字节、采样率、单声道、小端序解析和清理行为；
- 不支持或授权失败时进入内置示例，不伪造麦克风结果。

W2 硬门禁：

- 安静、正常说话、持续低频、约 1 kHz 中频和拍手必须产生明显不同的数据与画面；
- 基础构图位置保持稳定，但形态尺寸、色带振幅、粒子密度和扰动速度应有肉眼可见差异；
- 任一目标设备出现“图案几乎不变化”，必须保留输入摘要并继续修正，不能仅靠文案表示响应；
- 临时文件删除必须显示可验证的成功状态；删除失败、状态不确定或仍占用麦克风均阻塞；
- 当前 Android 结果必须在最新修复构建上重新完整验证；
- iPhone 尚未验证，在取得真实设备记录前不得宣称 W2 真机验收或双平台通过；
- `npm run test`、`npm run build` 和 `npm run build:miniprogram` 必须全部成功。

#### W3：导出与分享（实现完成，真机门禁待验）

- 使用离屏 Canvas 合成 1080×1440 PNG；
- 支持用户主动保存相册、微信图片分享、取消分享和失败兜底；
- 导出包含作品、时间、Mood、三层比例和三个标签，文字无裁切；
- 导出结束后释放高分辨率 Canvas 和临时图片文件；
- 加入相册写入能力前同步更新隐私草案和公众平台指引。

#### W4：真机与审核 QA（本机准备完成，真机门禁待验）

- 在微信开发者工具、Android 微信和 iPhone 微信验证权限、隐私、后台释放、横竖屏、
  重复聆听、导出、保存、分享取消、低端机性能和包体；
- 中低端 Android 连续完成至少十次，不崩溃、不卡死、不持续占用麦克风；
- 完成体验版验收、审核截图、文案和功能路径说明；
- 无 iPhone 设备时只能保留明确门禁，不得用模拟器替代真机结论。

#### W5：备案、提审和发布（仓库准备完成，外部门禁待管理员）

- 核对个人主体实际可用且与功能真实匹配的类目；
- 完成小程序备案与用户隐私保护指引；
- 使用微信开发者工具上传版本并设置体验版；
- 完成 Android/iPhone 最终验收后在公众平台提交审核；
- 审核通过后由管理员手动发布；
- 没有备案、审核或管理员发布凭证时，只能准备材料，不能声称已经上架。

每个 `Wx` 的实现必须先完成自动测试和构建，再独立 commit 并推送；真机、备案、审核等
只能由外部环境完成的门禁必须保留到最终验收并在 W5 发布前全部关闭。本轮用户明确要求
连续完成后续实现，因此可以在如实记录未验证门禁后继续下一阶段，但不得把未验证写成通过，
也不得把相邻里程碑或 W2 与第 19 节 AI 改动混在同一提交。

### 18.5 外部依赖与合规证据

以下状态表只记录证据，不替代平台后台或官方材料。涉及规则变更时只使用官方来源复核。
“用户确认”不是平台发布凭证；“未核对”项目在对应里程碑开始时必须补日期和链接。

| 项目 | 最近核对 | 当前状态 | 官方来源或发布前证据 |
|---|---|---|---|
| 微信账号与名称 | 2026-07-27，用户确认 | 已准备 | 公众平台后台截图或管理员确认 |
| 服务类目 | 2026-07-27，用户确认 | 已配置多个工具类目 | 公众平台实际类目列表与提审选择记录 |
| AppID | 2026-07-27，用户确认 | 已取得，仅本机使用 | 私有配置可导入，Git 中不存在真实值 |
| RecorderManager | W2 开始时 | 实现中 | [微信官方 RecorderManager](https://developers.weixin.qq.com/miniprogram/dev/api/media/recorder/RecorderManager.html) 与双平台真机记录 |
| 隐私授权 | W2 开始时 | 草案已准备 | [微信官方隐私授权](https://developers.weixin.qq.com/miniprogram/dev/framework/user-privacy/PrivacyAuthorize.html) 与公众平台最终指引 |
| 麦克风与临时文件 | W2 最新构建尚未完成复验 | 进行中 | Android/iPhone 设备、系统、微信版本及清理记录 |
| 相册写入 | W3 实现时 | 已实现、真机待验 | 官方相册权限说明、拒绝与撤回测试、隐私指引更新 |
| 小程序备案 | 未核对 | 未确认完成 | 工信部与公众平台当期备案要求、平台完成凭证 |
| 体验版与审核 | 未核对 | 未开始 | 官方发布流程、上传版本号、体验记录、审核结果 |
| AI 标识法规 | 未核对 | 未来 AI 门禁 | 届时有效的官方法规、标准及独立导出检查 |
| AI 模型供应商 | 未核对 | 候选、未验证 | 官方控制台与协议、正式账户、地域、保留条款、价格、压测和盲评 |

---

## 19. 已批准的 AI 延伸路线

本节记录用户于 2026-07-27 批准的后续产品与技术路线，全部属于 W5 之后的未来阶段。
它不授权跳过当前小程序里程碑，也不允许把未提交的 W2 工作与 AI 改动混在同一提交。
AI 里程碑开始后，本节只在明确标注的 AI 模式内覆盖 Web 和 W0–W5 的冲突范围；
Web 与小程序本地模式“不上传原始声音”的事实始终不变。

W5 的仓库准备已完成，但 iPhone、备案、隐私平台配置、审核和管理员发布仍是外部门禁。
按用户当前要求可以继续实现默认关闭的 AI 代码与 mock 链路；在这些门禁关闭、真实供应商
凭证与 AI 合规材料齐备前，AI 入口不得对用户开启，也不得上传声音或调用真实模型。

AI 规范中的模型名、性能和成本数字是规划候选，不是已经验证的生产事实：

- Qwen3-Omni Captioner 是声音理解候选；
- Z-Image-Turbo 是默认图像候选；
- 腾讯混元是 AI-M4 同条件盲测候选；
- 只有正式账户、官方能力、数据保留条款、地域、成本和真机压测均通过后才能定为供应商。

### 19.1 两档体验

以下表是第 19 节关于网络、账户、上传和成本的唯一硬边界；后续章节只说明实现方式，
不得产生不同解释：

| 边界 | 本地声纹画 | AI 声音画 |
|---|---|---|
| 项目网络请求 | 必须为零 | 仅在明确点击 AI 生成后允许 |
| 账户 | 无 | 点击 AI 生成后才可无感建立 |
| 声音上传 | 不允许 | 点击 AI 生成、额度与预算检查通过后临时上传十秒 PCM |
| 五个数值、Mood、seed | 不上传 | 仅随已授权 AI 任务临时处理 |
| 生成成本 | 零 API 成本、无限次 | 成功作品结算一次额度 |
| 失败兜底 | 保持本地作品 | 释放未结算额度并回退本地作品 |
| 默认持久化 | 无声音、声纹或作品历史 | 只保留第 19.16–19.17 节必要任务与额度元数据 |

1. **本地声纹画（免费版）**
   - 录音、特征提取、预设选择、绘制、保存与分享都在小程序本地完成；
   - 不上传原始声音，也不上传五个数值、Mood、seed 或任何创作请求；
   - 不调用声音模型、图像模型、业务 API、对象存储或 CDN；
   - 不要求账户、不扣额度、不限生成次数，项目侧单次生成 API 成本为零；
   - 使用项目包内置的构图预设和本地程序化纹理，延续当前 MVP 技术路线并扩大变化范围；
   - 同时作为离线、隐私、预算耗尽与 AI 服务失败时的兜底。
2. **AI 声音画（完整声音理解版）**
   - 用户明确授权后临时上传十秒 PCM；
   - 由声音理解模型生成结构化声音描述，再调用图像模型；
   - 必须使用微信无感账户并消耗生成额度；
   - 失败时不得丢失当前本地作品，应退还未实际消耗的额度并允许回退免费版。

不得再建立“只上传五个数值但仍调用图像 API 的免费版”。五个数值在免费版中只用于
本地绘图；以后即使增加匿名统计，也必须作为单独的可选数据分析功能审批，不能成为生成依赖。

### 19.2 免费版的完全本地技术链路

```text
小程序麦克风
  → 本地接收十秒 PCM 帧
  → 每帧计算响度、低/中/高频能量与变化率
  → 得到五个汇总值 + 仅驻留内存的时间声纹
  → 声纹哈希选择内置构图预设并生成确定性 seed
  → Mood 调整配色、明暗、扩张方向与材质感
  → Canvas 2D 程序化绘制
  → 本地导出、保存与分享
```

实现约束：

- 继续保留 `loudness`、`lowEnergy`、`midEnergy`、`highEnergy`、`changeRate`
  五个 0..1 汇总值，作为整体形态控制；
- 在十秒录音中按固定窗口保留约 16–24 组五维时间采样，只驻留当前页面内存，
  用于表达渐强、脉冲、间歇、持续与频段迁移；录音结束后不得写入远端；
- 预设不是预制成品图片，而是内置的参数化构图配方。首个增强版本至少覆盖同心场、
  流动带、散点星群、层叠纸片、纵向雨幕、径向脉冲、碎片网格与平静地平线；
- 先用时间声纹的稳定哈希选择预设和 seed，再把五个汇总值与时间采样映射到图形数量、
  尺寸、间距、曲率、密度、颗粒、对比度和局部运动；
- 同一段录音与同一 Mood 应得到稳定、可复现的主体构图；不得把 `Date.now()` 作为主要 seed；
- Mood 只改变艺术解释，不覆盖声纹结构，切换 Mood 后仍应能看出是同一段声音；
- 纹理优先使用 Canvas 噪声、颗粒、抖动、透明叠色等程序化效果；如使用小型纹理素材，
  必须随小程序包内置，不在生成时下载；
- 对外称为“按声纹生成”或“本地声音可视化”，不得声称免费版识别出了雨、人、鸟、
  车辆等真实声源；
- 运行时按设备像素比绘制但将内部像素比上限设为 2，避免低端手机内存和导出时间失控；
- 免费版的本地分享图可导出为 1080×1440；复杂纹理可先在较低分辨率绘制后再叠加
  高分辨率矢量线条和文字。

### 19.3 AI 版默认技术链路

```text
小程序
  → 上传十秒临时 PCM
国内无状态服务
  → Qwen3-Omni-30B-A3B-Captioner
  → 得到结构化声音描述
  → 立即删除临时音频
  → 结合本地响度、频段、变化率、Mood 与 seed 生成艺术提示词
  → Z-Image-Turbo
  → 压缩成展示图
  → 返回小程序
```

约束：

- Qwen3-Omni Captioner 与 Z-Image-Turbo 是默认评估候选；正式选用时必须具有可用的
  中国内地服务地域，并通过第 19.9、19.21、19.23 和 19.24 节门禁；
- API 密钥只能保存在服务端，不得写入小程序、前端包或 Git；
- 声音描述只输出场景、声源、材质、动态、空间感与置信度等结构化字段；
- 默认不返回逐字转写；服务端必须过滤模型意外返回的可识别人声内容；
- 图像提示词同时使用结构化声音描述与五个本地数值，不能让语义描述完全覆盖声音指纹；
- 每次用户动作默认只生成一张图；重新生成必须是新的显式动作并单独计入额度；
- 展示优先返回压缩 WebP/JPEG；用户保存时再合成目标尺寸 PNG。

### 19.4 临时音频生命周期

- 小程序以二进制方式上传临时 PCM，避免在用户侧 Base64 膨胀；
- 无状态服务不得建立音频历史；优先在内存中转发给声音理解服务；
- 如果供应商接口需要 Base64，由服务端临时编码，不增加用户上行流量；
- 如果必须使用临时对象 URL，对象必须使用不可预测名称、最小权限与短时签名；
- Captioner 返回、请求失败、超时或取消后都必须主动删除临时音频；
- 额外设置不超过 10 分钟的自动过期兜底，并只记录删除状态，不记录音频内容；
- 未核实供应商数据保留条款前，不得对外宣称“云端零保留”或“立即从所有系统删除”。

### 19.5 微信账户与额度

AI 声音画需要最小账户来防止滥用和控制成本，但不采用传统注册表单：

- 使用微信登录能力建立无感账户，以服务端换取的微信用户标识映射内部 `user_id`；
- 用户完成录音与 Mood 选择、首次点击“AI 生成作品”时才触发微信无感建号；
  仅选择 AI 模式、阅读说明或录音不得建号，本地声纹画始终不要求登录；
- 首阶段不收集手机号、邮箱、密码、头像、昵称或通讯录；
- 仅持久化内部用户标识、加密或受保护的微信标识、额度余额、额度流水、
  生成次数、账号状态与必要时间戳；
- 不默认保存原始声音、声音描述、提示词或作品历史；
- 每个生成请求必须有幂等 ID，重试不能重复扣额度或重复调用图像 API；
- 用户明确点击“AI 生成作品”且本地有效性检查通过后，先预占一次额度，再上传音频并
  发起 Captioner；成功返回作品后结算，失败或最终超时释放预占额度；
- 调用失败不扣额度；已产生图像模型费用但客户端断开时，按可恢复结果处理；
- 免费赠送额度、每日上限、月度上限和项目每日硬预算均由服务端配置，不写死在小程序；
- 达到个人或项目上限时，清楚提示并回退本地声纹画；
- 首个 AI 里程碑只建立免费额度体系，不同时引入微信支付；付费额度需在成本、
  留存与转化验证后单独批准。

### 19.6 成本控制

- 安静、过短、损坏或无有效帧的录音必须在本地拦截，不调用模型；
- 用户选定 Mood 并点击明确显示额度消耗的按钮后，才允许上传声音或调用任何付费模型；
- 对声音理解、提示词生成和图像生成分别记录调用次数、耗时、供应商请求 ID 与费用，
  但不记录用户声音内容；
- 服务端必须设置单用户限速、每日/月度额度、全项目日预算、并发上限与供应商超时；
- 预算耗尽、供应商限流或服务异常时自动降级，不得无限重试；
- 同一请求最多进行一次受控的供应商重试；内容安全拒绝不得自动反复生成；
- 初期按每张成功作品不高于人民币 0.15 元规划预算，超过后必须复盘模型、画质与重试率。

### 19.7 AI 延迟与交互预算

十秒录音是创作过程，不计入“点击生成后的等待时间”。首版按以下目标设计，具体数值必须用
中国内地真机、真实网络和正式供应商账户实测，不得把模型推理时间当作端到端耗时：

| 阶段 | 首版规划目标 | 优化方式 |
|---|---:|---|
| 上传约 320 KB PCM | 0.3–2 秒 | 二进制直传、同地域服务、避免客户端 Base64 |
| Qwen3-Omni Captioner | 1–4 秒 | 固定短输出 JSON、限制描述长度、设置超时 |
| 提示词组装 | 0.1 秒内 | 服务端确定性模板，不再调用第二个语言模型 |
| Z-Image-Turbo 端到端 | 2–6 秒 | 8 步 Turbo、关闭提示词扩写、控制并发与排队 |
| 转码与返回 | 0.3–1.5 秒 | 服务端转 WebP/JPEG、只返回展示尺寸 |

交互与失败策略：

- 用户进入 AI 录音前先阅读并同意临时上传说明，但此时不建号、不上传、不调用模型；
- 录音十秒期间只在本地计算五个数值，录音结束先做静音、损坏和过短检查；
- 用户选择 Mood 后，按钮必须显示“AI 生成作品 · 成功后消耗 1 次额度”；
- 只有点击该按钮后才无感建号、预占额度、上传 PCM，并顺序执行 Captioner 与图像生成；
- 首版不在录音过程中分片上传：十秒 PCM 约 320 KB，提前流式上传节省有限，
  却会显著增加隐私说明、断线重组与服务端状态管理复杂度；
- 点击“AI 生成作品”后的体感目标为中位数不超过 8 秒、P95 不超过 15 秒；
  正常网络下多数请求的规划范围为 5–12 秒；这些数值必须由正式服务压测确认；
- Captioner 超过 6 秒或失败时，不调用图像模型、不扣额度，直接提供本地声纹画；
- 图像任务超过 12 秒时不得重复发起；显示“仍在生成，可稍后返回”，通过幂等任务 ID
  恢复结果。超过最终超时仍无结果时按额度结算规则退款；
- 等待界面先显示本地声纹动画；结构化描述返回后，只展示经过过滤的简短环境理解，
  让等待过程产生可感知进展。

### 19.8 手机展示画质与传输

- AI 基础图默认采用 3:4 竖幅 864×1152，不追求 2K/4K；
- Z-Image-Turbo 关闭额外提示词扩写，由 Sound Palette 自己的提示词模板控制风格与构图；
- 供应商返回 PNG 后，服务端转为质量合适的 WebP 或 JPEG，展示文件目标约 150–350 KB；
- 小程序收到 AI 基础图后，可在本地 Canvas 叠加由五个声音数值驱动的清晰矢量线条、
  颗粒、日期和品牌信息，再导出 1080×1440 分享图；
- 这种“低分辨率 AI 底图 + 本地高分辨率声纹层”是默认方案：既压缩生图和下载时间，
  又避免抽象画在手机全屏查看时显得模糊，并保留声音与作品之间的可解释联系；
- 只有盲测证明 864×1152 在目标手机上明显不足时才提高生成分辨率，不以桌面放大查看
  作为升级画质的理由。

### 19.9 混元画质对照

Z-Image-Turbo 是默认候选，但正式确定前必须与腾讯混元进行同条件盲测：

- 使用同一批结构化声音描述、五个数值、Mood、864×1152 画幅与艺术方向；
- 至少覆盖安静、雨声、街道、人声环境、自然声、音乐、突发声与持续低频；
- 每类覆盖三种 Mood，不使用真实用户隐私音频作为公开测试素材；
- 主对照为同成本档的混元生图极速版；
- 小样本增加混元生图 2.0，用于判断提高单张成本能否带来显著画质收益；
- 评价维度固定为作品吸引力、声音关联感、构图变化、风格稳定性、生成耗时、
  失败率、单张成本和可分享性；
- 采用隐藏模型名称的盲评，不得只凭团队知道供应商后的主观偏好选型；
- 画质对照只用于离线评测，不得让每个真实用户请求同时调用两个图像模型。

### 19.10 产品定义与成功标准

后续小程序产品只提供两个明确入口：

| 产品档位 | 用户名称 | 生成位置 | 是否上传声音 | 是否需要账户 | 用户成本 |
|---|---|---|---|---|---|
| 免费版 | 本地声纹画 | 用户手机 Canvas 2D | 否 | 否 | 无限次、零额度 |
| AI 版 | AI 声音画 | 手机本地分析 + 国内云端声音理解与生图 | 明确确认后临时上传十秒 PCM | 是 | 成功作品消耗 1 次 AI 额度 |

产品成功不以“接入了模型”为标准，而以以下用户结果为标准：

- 新用户不阅读说明也能分清免费版与 AI 版的差异；
- 免费版相同录音之间有稳定关联，不同声音之间有明显构图变化；
- AI 版能给出比免费版更具体、更有变化的声音语义与抽象画面；
- 用户知道何时上传、何时消耗额度，失败时不会产生被误扣的感受；
- 分享图在主流手机全屏查看时清晰、有吸引力，并保留声音与作品的关系；
- 免费版一次完整生成不发起任何项目网络请求；
- AI 服务超时、限流或预算耗尽时，用户仍能完成本地作品。

首个 AI 版本的核心衡量指标：

- 免费流程完成率；
- AI 入口点击率；
- AI 授权后生成确认率；
- AI 成功率；
- 点击 AI 生成到结果出现的 P50/P95；
- 成功作品单张成本；
- 保存率与分享发起率；
- AI 版相对免费版的盲评偏好率。

免费版不接远端埋点，因此免费流程指标只允许通过测试样本、用户访谈或以后单独批准的
可选统计获得，不能为了指标破坏零网络边界。

### 19.11 范围与非目标

首个双模式版本必须完成：

- 微信原生小程序，保留单页状态机；
- 十秒录音；
- 三种 Mood：`good`、`neutral`、`low`；
- 本地五个汇总特征与 16–24 段时间声纹；
- 至少八套本地参数化构图配方；
- 本地免费生成、换 Mood、重听、保存与分享；
- AI 临时上传授权、微信无感账户、额度查询与额度流水；
- Qwen3-Omni Captioner 声音理解；
- Z-Image-Turbo 单图生成；
- AI 结果恢复、失败退款和本地降级；
- 864×1152 AI 基础图和 1080×1440 分享图；
- AI 生成内容显式标识、隐式标识技术验证和隐私披露；
- Z-Image-Turbo 与腾讯混元离线对照。

首个双模式版本明确不做：

- 不在 Web 版接入 AI；
- 不在手机上安装或运行本地神经网络模型；
- 不上传免费版的声音、五个数值、时间声纹、Mood 或作品；
- 不做逐字转写、说话人识别、声纹身份识别或人声内容存档；
- 不默认保存录音、声音描述、提示词或历史作品；
- 不做作品社区、关注、评论、排行榜或公开画廊；
- 不做聊天式改图、局部重绘、图生图或一次生成多张候选；
- 不做模型微调；
- 不在首个 AI 里程碑接入微信支付、订阅、广告或邀请裂变；
- 不为了降低延迟在录音过程中分片上传；
- 不把开发者桌面电脑作为生产服务器；
- 不在真实用户请求中同时调用 Z-Image 与混元。

### 19.12 信息架构与状态机

本节从 AI-M2 起生效。AI-M2 已提供这些状态的类型与离线 mock 服务；小程序正式入口仍由
默认关闭的 feature flag 阻止，W2–W5 的本地版页面、账户和零网络事实不变。

继续使用 `pages/home/index` 单页，不为了双模式引入路由框架。页面状态扩展为：

```ts
type AppState =
  | 'home'
  | 'ai-consent'
  | 'listening'
  | 'mood'
  | 'ai-auth'
  | 'ai-generating'
  | 'result'
  | 'recoverable-error';

type CreationMode = 'local' | 'ai';
```

状态流：

```text
home
  ├─ 本地声纹画
  │    → listening(local)
  │    → mood(local)
  │    → result(local)
  │
  └─ AI 声音画
       → ai-consent
       → listening(ai，仍只在本地)
       → mood(ai)
       → 点击“AI 生成作品 · 成功后消耗 1 次额度”
       → ai-auth
       → ai-generating
       → result(ai)

任何 AI 失败
  → recoverable-error
  → result(local) 或恢复 AI 任务
```

共同约束：

- `creationMode` 必须随当前创作会话保存，不能通过按钮文案猜测；
- 切后台时停止录音并释放监听器；
- 本地结果和 AI 结果必须在视觉与文案上可辨认；
- 返回上一状态不得自动上传、自动扣额度或自动重新生成；
- 页面刷新、进程回收或网络断开后，只恢复已创建的 AI `job_id`，不得重新调用供应商。

### 19.13 页面与交互规格

#### 19.13.1 首页 `home`

必须显示：

- 品牌名与一句核心文案；
- “本地声纹画”主入口；
- “AI 声音画”次入口；
- 两档差异的短说明；
- “先体验示例”入口；
- 当前版本的隐私入口。

建议文案：

- 本地版：`免费 · 声音不离开手机 · 无限次`
- AI 版：`临时上传 10 秒声音 · AI 听懂环境 · 消耗额度`

行为：

- 进入首页不得请求麦克风、不得调用 `wx.login`、不得访问业务 API；
- 点击本地版后才进入本地权限流程；
- 点击 AI 版先进入 `ai-consent`，不直接请求麦克风；
- 首页不得用“免费 AI”描述本地声纹画。

#### 19.13.2 AI 说明与授权 `ai-consent`

首次进入 AI 版和隐私版本更新后必须显示；同一隐私版本可在本机记住已阅读状态。

必须说明：

- 录音时长为十秒；
- 只有最终点击 AI 生成按钮才会临时上传；
- 声音用于生成结构化环境描述，不默认逐字转写；
- Captioner 返回或失败后立即主动删除临时音频，并有不超过十分钟的自动过期兜底；
- 成功作品消耗一次额度，失败不扣；
- 保存或分享 AI 作品时，候选分享图会被临时送到服务端添加和校验法定 AI 标识，
  该过程不再接触原始声音；
- 用户随时可以改用完全本地版。

按钮：

- 主按钮：`我知道了，继续`
- 次按钮：`改用本地声纹画`
- 链接：`查看完整隐私说明`

此页面的同意只允许进入 AI 录音流程，不代表已经同意扣额度，也不允许提前上传。

#### 19.13.3 聆听 `listening`

两档共用同一视觉结构：

- 十秒倒计时；
- 实时本地声纹动画；
- 当前模式标记；
- 停止与取消；
- 麦克风权限错误处理。

本地版：

- 录音帧只进入本地分析器；
- 结束后删除临时文件并进入 Mood。

AI 版：

- 此阶段行为仍与本地版相同，不上传、不建号、不扣额度；
- 录音完成后保留临时 PCM 路径到用户点击生成或取消；
- 用户退出、切换本地模式、页面卸载或等待超过五分钟时删除临时文件；
- 静音、损坏、过短或帧缺失必须在此阶段拦截。

#### 19.13.4 Mood `mood`

问题保持：`此刻，你感觉怎么样？`

三个选项：

- `好`
- `不好不坏`
- `不好`

本地版：

- 主按钮：`生成本地声纹画`
- 点击后只运行本地 Canvas，不需要账户和网络。

AI 版：

- 有本机缓存时可显示“上次同步剩余额度”；未建号或无缓存时显示 `将在生成时获取额度`，
  最终余额必须以用户点击生成后的服务端检查为准；
- 主按钮：`AI 生成作品 · 成功后消耗 1 次额度`
- 按钮下方：`点击后才会临时上传这段 10 秒声音`
- 不允许用模糊的“继续”按钮代替费用与上传确认。

#### 19.13.5 AI 建号与生成 `ai-auth` / `ai-generating`

点击 AI 生成按钮后按顺序执行：

1. 获取或恢复微信登录态；
2. 服务端建立最小账户；
3. 检查个人额度、限速与项目预算；
4. 预占一次额度；
5. 上传 PCM；
6. Captioner 返回结构化声音描述；
7. 删除音频；
8. 组装提示词；
9. 调用图像模型；
10. 压缩并返回结果；
11. 成功结算额度。

进度文案只允许反映真实状态：

- `正在安全上传这段声音`
- `正在理解环境里的声音`
- `正在把声音变成画面`
- `正在整理分享图片`

禁止显示虚假百分比。可使用循环动画和阶段切换。用户关闭页面后任务继续时，必须提供
`稍后回来查看`，不得因客户端断开再次调用模型。

#### 19.13.6 结果页 `result`

共同内容：

- 作品；
- Mood；
- 三层频段构成；
- 三个声音标签；
- 再听一次；
- 保存图片；
- 分享；
- 回到首页。

本地版额外要求：

- 标注 `本地声纹生成 · 声音未上传`；
- `换一种 Mood` 免费且不改变主体构图；
- 可提供 `让 AI 听懂这段声音`，但只有当前 PCM 尚未删除且用户重新确认时才可继续；
  首版默认不保留 PCM，因此该入口应重新录音。

AI 版额外要求：

- 清楚标注 `AI 生成`；
- 展示经过过滤的短声音理解，例如 `连续细雨、远处车辆、空间开阔`；
- 不显示逐字转写、不显示置信度小数或内部提示词；
- `换 Mood 再生成` 与 `再生成一张` 都必须再次显示消耗一次额度并要求点击确认；
- 同一 `caption_token` 在十分钟有效期内可避免重新上传和重新理解，但每次新图片仍消耗一次额度；
- 保存和分享的文件必须保留 AI 显式标识，并通过隐式标识发布门禁。

### 19.14 本地视觉配方规格

本节接口已从 AI-M1 起生效。历史 W2 提交只保留五个汇总值与既有固定布局；时间声纹、
稳定哈希和八套配方均位于独立 AI-M1 提交，不反向改写 W2 的验收事实。

本地配方接口：

```ts
interface SoundFeatureSample {
  t: number; // 0..1
  loudness: number;
  lowEnergy: number;
  midEnergy: number;
  highEnergy: number;
  changeRate: number;
}

interface LocalSoundFingerprint {
  version: 1;
  samples: SoundFeatureSample[]; // 16..24
  summary: SoundSummary;
  hash: string;
}

type RecipeId =
  | 'concentric-field'
  | 'flowing-ribbons'
  | 'particle-constellation'
  | 'layered-paper'
  | 'vertical-rain'
  | 'radial-pulse'
  | 'fractured-grid'
  | 'calm-horizon';

interface LocalArtworkManifest {
  version: 1;
  mode: 'local';
  recipeId: RecipeId;
  seed: string;
  mood: Mood;
  summary: SoundSummary;
  fingerprintHash: string;
  createdAt: number;
}
```

配方要求：

- 配方选择必须由声纹哈希与结构特征共同决定，不能纯随机；
- 八套配方必须使用统一的颜色、字体与材质系统，不能像八个无关模板；
- 时间声纹至少影响一个空间维度和一个密度或节奏维度；
- 同一 `fingerprintHash + mood + recipeVersion` 的静态导出必须可复现；
- 配方版本升级必须改变 `version`，避免旧结果无法解释；
- `Date.now()` 只允许记录创建时间，不允许作为主要构图 seed；
- 示例模式必须提供覆盖八套配方的固定 fixture，供开发与回归测试；
- 配方与程序化纹理新增发布包体目标不超过 250KB；
- 不内置声音样本库、预生成声纹文件或神经网络权重。

### 19.15 AI 声音描述与提示词数据

本节接口从 AI-M3 起生效。当前仓库已实现结构校验、隐私过滤、一次受控修复和默认关闭的
Qwen 候选 transport；自动测试只使用合成字节与固定 mock，不接触真实声音或供应商。

Captioner 输出必须先由服务端验证为以下结构，禁止把模型自由文本直接送给图像模型：

```ts
interface StructuredSoundCaption {
  version: 1;
  environment: string[];      // 室内、街道、自然等，最多 3 项
  sources: string[];          // 雨、车辆、脚步等，最多 5 项
  dynamics: string[];         // 连续、间歇、渐强等，最多 4 项
  materials: string[];        // 柔软、金属、颗粒等艺术映射，最多 4 项
  spatial: string[];          // 近、远、开阔、封闭等，最多 3 项
  confidence: 'low' | 'medium' | 'high';
  containsSpeech: boolean;
}
```

过滤规则：

- 每个字符串限制长度并去除姓名、电话、地址、账号、逐字语句和其他可识别内容；
- `containsSpeech=true` 时只允许保留“近处有人声”这类环境级描述；
- 不允许返回说话人身份、性别推断、年龄推断、情绪诊断或谈话内容；
- 低置信度结果必须使用保守艺术描述，不把猜测写成事实；
- JSON 解析失败只允许一次受控修复；仍失败则释放额度并降级本地版；
- 提示词由本地模板生成，输入为结构化声音描述、五个汇总值、Mood、配方方向和品牌风格；
- 首版不得再调用第二个大语言模型扩写提示词；
- Z-Image `prompt_extend` 必须关闭。

### 19.16 服务端持久化数据

只建立完成账户、额度、幂等和故障恢复所需的数据。

`users`：

| 字段 | 说明 |
|---|---|
| `user_id` | 内部随机 ID |
| `wechat_subject` | 受保护的 OpenID 映射，不返回客户端 |
| `status` | active / limited / deleted |
| `consent_version` | 最近同意的 AI 隐私版本 |
| `consented_at` | 同意时间 |
| `created_at` / `updated_at` | 必要时间戳 |

`credit_accounts`：

| 字段 | 说明 |
|---|---|
| `user_id` | 用户 |
| `available` | 可用额度 |
| `reserved` | 进行中预占 |
| `lifetime_granted` | 累计赠送 |
| `lifetime_spent` | 累计成功消耗 |
| `version` | 乐观锁版本 |

`credit_ledger`：

| 字段 | 说明 |
|---|---|
| `ledger_id` | 流水 ID |
| `user_id` / `job_id` | 关联对象 |
| `type` | grant / reserve / settle / release / adjustment |
| `amount` | 正整数 |
| `reason_code` | 固定枚举，不写声音内容 |
| `created_at` | 时间 |

`generation_jobs`：

| 字段 | 说明 |
|---|---|
| `job_id` | 客户端生成的幂等 UUID |
| `user_id` | 用户 |
| `status` | created / captioning / imaging / finalizing / succeeded / failed / expired |
| `mood` | 三值枚举 |
| `summary_numbers` | 五个 0..1 临时控制值；任务结束后必须清空 |
| `caption_model` / `image_model` | 实际模型版本 |
| `provider_request_ids` | 供应商请求 ID，不含输入内容 |
| `latency_ms` | 分阶段耗时 |
| `cost_micros` | 分阶段估算成本 |
| `error_code` | 固定错误码 |
| `result_expires_at` | 临时结果过期时间 |
| `created_at` / `updated_at` | 时间 |

禁止建立：

- 原始音频历史表；
- 逐字稿表；
- 可搜索的声音描述库；
- 默认作品相册表；
- 免费版行为日志；
- 用户画像、广告标签或联系人关系表。

`summary_numbers` 只属于 AI 请求的临时服务端控制字段，不改变免费版零上传规则，也不得
随 90 天任务元数据继续保留。

### 19.17 临时数据与保留期限

| 数据 | 存放位置 | 默认期限 | 结束动作 |
|---|---|---:|---|
| 免费版 PCM 帧 | 手机内存 | 录音期间 | 生成摘要后释放 |
| 免费版时间声纹 | 手机内存 | 当前创作会话 | 离页或重录后释放 |
| AI 临时 PCM 文件 | 手机临时目录 | 最长 5 分钟等待确认 | 取消、离页、超时或上传完成后删除 |
| 服务端 AI PCM | 内存或临时对象 | Captioner 返回即删，最长 10 分钟 | 主动删除 + TTL |
| 结构化声音描述 | 短期缓存 | 最长 10 分钟 | 到期删除 |
| 艺术提示词 | 进程内存或短期缓存 | 最长 10 分钟 | 到期删除 |
| AI 展示图片 | 临时对象存储 | 最长 60 分钟 | TTL 删除 |
| AI 候选分享图 | 服务端内存或临时对象 | 完成 `finalize` 即删，最长 10 分钟 | 主动删除 + TTL |
| AI 五个控制数值 | 任务内存或任务临时字段 | 任务终态或最长 60 分钟 | 清空字段 |
| AI 任务元数据 | 数据库 | 90 天 | 到期清理或聚合 |
| 额度流水 | 数据库 | 首版 180 天 | 付费上线前重新审查法定期限 |
| 运维日志 | 日志系统 | 30 天 | 自动删除 |

日志禁止包含：

- 音频二进制或 Base64；
- Captioner 原始输出；
- 完整提示词；
- 生成图片二进制或永久 URL；
- OpenID 明文；
- 用户可能说出的任何句子。

客户端与服务端都必须记录删除是否完成，但删除日志只写 `job_id`、时间、结果与错误码。

### 19.18 API 契约

本节从 AI-M2 起生效。当前实现是内存 mock 契约与测试，不是已部署的 HTTPS 服务；它已
将任务元数据创建与模拟音频接收拆成两个显式步骤。真实服务落地时仍必须保证额度和预算
检查发生在上传之前，不得把 PCM 放进首次建号或额度检查请求。

首版 API 最小集合：

```text
POST /v1/auth/wechat
GET  /v1/account/credits
POST /v1/generations
PUT  /v1/generations/{job_id}/audio
GET  /v1/generations/{job_id}
POST /v1/generations/{job_id}/cancel
POST /v1/generations/{job_id}/regenerate
POST /v1/generations/{job_id}/finalize
```

要求：

- 全部使用 HTTPS；
- `AppSecret`、模型密钥和对象存储凭证只在服务端；
- 客户端使用业务短期 token，不保存 OpenID 或 `session_key`；
- `POST /v1/generations` 只提交 `job_id`、`consent_version`、Mood、五个数值、
  音频格式版本和客户端版本，不包含音频；
- 相同 `job_id` 重复请求必须返回同一任务，不得再次预占或调用模型；
- 服务器完成认证、同意版本、参数、用户限速、项目预算和原子额度预占后，
  返回 `202 Accepted`、`job_id` 与短期一次性上传授权；
- `PUT /v1/generations/{job_id}/audio` 才使用二进制上传 PCM，不在客户端 Base64；
- 音频上传只接受已预占、未取消、未上传且授权未过期的任务；重复上传不得再次调用模型；
- 客户端以有限轮询获取状态；后续可替换 WebSocket，但首版不需要；
- 所有接口使用固定错误码和可安全展示的中文消息；
- `regenerate` 只允许使用未过期的 `caption_token`，请求体必须携带新的 `job_id`；
  每个新任务单独预占额度，重放同一新 `job_id` 不得重复调用或扣费；
- `finalize` 用于生成合规分享文件，按源 `job_id + output_version` 幂等，
  重复调用返回同一结果且不重复扣额度。

异步服务端顺序必须是：

```text
POST 元数据：认证
→ 同意版本校验
→ 参数与声明音频格式校验
→ 用户限速
→ 项目预算检查
→ 原子预占额度
→ 建立任务并返回 202 与一次性上传授权

PUT 音频：校验任务与上传授权
→ 接收临时 PCM
→ 标记上传完成并进入异步队列

异步 worker：调用 Captioner
→ 删除音频
→ 图像模型
→ 结果落入短期存储
→ 结算额度
→ 客户端轮询得到终态
```

若上传授权过期、用户取消或客户端始终未上传，任务必须进入失败或过期终态并释放预占。
`caption_token` 只是服务端短期不透明引用，最长十分钟；客户端不能从中读取结构化描述，
到期后删除关联缓存。

### 19.19 额度、失败与取消

首版只做额度系统，不接真实支付：

- 新用户赠送额度、每日赠送、管理员补发均由服务端配置；
- 额度是 AI 成功作品次数，不与免费版本地生成次数关联；
- 本地版不得显示余额焦虑或诱导付费；
- 余额不足时在用户点击 AI 生成后提示，不得先上传再提示；
- 后续出售额度必须另立支付需求、退款规则、未成年人规则和财务保留期限。

结算规则：

| 情况 | 用户额度 |
|---|---|
| 本地校验失败 | 不预占 |
| 账户或预算检查失败 | 不预占 |
| Captioner 失败或超时 | 释放预占 |
| 内容安全拒绝 | 释放预占 |
| 图像模型失败或最终超时 | 释放预占 |
| 服务端已得到可恢复图片、客户端断开 | 正常结算，允许恢复 |
| 用户在模型调用前取消 | 释放预占 |
| 用户在模型已调用后取消 | 任务继续；成功则可恢复并结算，失败则释放 |
| 成功返回作品 | 结算 1 次 |

项目已经产生供应商费用但用户没有得到可恢复作品时，由项目承担，不得转嫁为用户额度损失。

### 19.20 错误码与用户降级

服务端最少支持：

| 错误码 | 用户提示方向 | 额度 | 下一步 |
|---|---|---|---|
| `AUTH_REQUIRED` | 需要重新连接微信账户 | 不预占 | 重试登录 |
| `CONSENT_REQUIRED` | 隐私说明已更新 | 不预占 | 回授权页 |
| `INVALID_AUDIO` | 这段声音未能完整读取 | 不预占/释放 | 重新录音 |
| `INSUFFICIENT_CREDITS` | AI 次数不足 | 不预占 | 使用本地版 |
| `USER_LIMITED` | 今日尝试较多 | 不预占 | 稍后或本地版 |
| `PROJECT_BUDGET_EXHAUSTED` | AI 今日暂时繁忙 | 不预占 | 本地版 |
| `CAPTION_TIMEOUT` | 暂时没能听懂 | 释放 | 本地版 |
| `IMAGE_TIMEOUT` | 画面仍在生成 | 保持预占 | 稍后恢复 |
| `CONTENT_REJECTED` | 这次无法完成 AI 生成 | 释放 | 本地版 |
| `PROVIDER_UNAVAILABLE` | AI 服务暂不可用 | 释放 | 本地版 |
| `RESULT_EXPIRED` | 临时作品已过期 | 已按原任务结算 | 重新录音或本地版 |

自动重试：

- Captioner 和图像模型各自最多一次；
- 只对明确的网络瞬断或供应商 5xx 重试；
- 内容拒绝、参数错误、额度不足和预算耗尽不得重试；
- 重试必须沿用同一 `job_id` 与供应商幂等能力；
- 客户端不得自行并发重发生成请求。

### 19.21 性能、包体与设备边界

免费版：

- Canvas 是微信运行时能力，不作为第三方软件或资源打包；
- 首屏主包目标不超过 1.5MB，新增配方代码与素材压缩后不超过 250KB；
- 不内置大图、声音样本、模型权重或通用图形引擎；
- 预览按实际显示尺寸绘制，内部像素比上限为 2；
- 1080×1440 只在保存时绘制一次，导出后释放高分辨率 Canvas；
- 低端机粒子总量、模糊半径和动画帧率必须有分级上限；
- 本地生成从点击按钮到静态结果出现目标低于 500ms；
- 录音后离线或飞行模式下仍可完成 Mood、生成与本地导出。

AI 版：

- PCM 采用 16kHz、单声道、16-bit，十秒约 320KB；
- AI 基础图 864×1152、单张；
- 展示图优先 WebP/JPEG，目标 150–350KB；
- 点击生成到结果 P50 ≤ 8 秒、P95 ≤ 15 秒；
- 超过 12 秒展示可离开提示，超过最终超时按任务状态处理；
- 不因提高桌面放大画质而升级到 2K/4K；
- 发布前必须在至少一台中低端 Android 与一台近五年 iPhone 真机验证。

### 19.22 AI 标识、内容安全与审核

AI 结果页和导出图片必须明确显示 `AI 生成`，不能只在隐私政策中说明。

发布前必须根据现行
[《人工智能生成合成内容标识办法》](https://www.nrta.gov.cn/art/2025/3/14/art_113_70340.html)
与 `GB 45438-2025` 完成合规核对：

- AI 图片适当位置具有用户可感知的显式标识；
- 下载、保存和分享文件仍保留显式标识；
- 文件元数据包含符合要求的隐式标识；
- 用户协议说明标识方法和用户不得恶意删除标识；
- 小程序提审材料如实说明提供 AI 生成服务；
- 不得把供应商已有水印当作完成全部合规义务的证据。

由于小程序 Canvas 二次导出可能移除图片元数据，AI 发布前必须验证完整导出链路。
默认实现为：

1. 结果页本地叠加可见 `AI 生成`；
2. 用户点击保存或分享时生成候选分享图；
3. 调用幂等 `finalize` 接口添加或验证显式标识与隐式元数据；
4. 只把通过校验的最终文件交给保存/分享能力；
5. `finalize` 不接触原始声音。

若无法证明最终文件保留所需标识，AI 保存与分享不得发布。

内容安全：

- 服务端在调用供应商前后执行必要的输入与输出安全检查；
- 不保存被拒绝的声音内容或提示词；
- 用户不可编辑内部提示词绕过安全策略；
- 安全拒绝使用中性文案，不推断用户意图；
- 免费本地抽象画不经过远端内容审核，但仍不得提供用户文本提示输入。

### 19.23 测试与验收

#### 免费版自动化验收

- 声音摘要、时间声纹、哈希与配方选择有单元测试；
- 八套 fixture 分别稳定命中八套配方；
- 同一 fixture、Mood 与版本重复生成得到相同清单；
- 切换 Mood 保留 `recipeId`、seed 与主体布局；
- `Date.now()` 变化不影响主体构图；
- 静音、帧缺失、过短、取消和切后台均释放录音资源；
- 本地模式端到端测试拦截所有 `wx.request`、上传与登录调用，调用次数必须为零；
- 导出完成后高分辨率 Canvas 与临时文件按规则释放。

#### 免费版人工验收

- 安静、说话、持续低频、拍手、雨声样本至少五类有明显视觉差异；
- 未经说明的测试者能说出免费版“不上传、无限次”；
- 八套构图属于同一品牌风格，不像模板拼盘；
- 中低端 Android 连续生成十次不崩溃、不卡死、不持续占用麦克风；
- 飞行模式下完成录音后的本地生成、换 Mood 和保存；
- 首屏加载与包体通过微信开发者工具分析。

#### AI 自动化验收

- `job_id` 幂等测试证明重复请求只扣一次、只调用一次图像模型；
- 额度预占、结算、释放和并发更新有事务测试；
- 点击 AI 生成前不发生业务登录、上传或模型调用；
- Captioner JSON 校验与人声内容过滤有固定样本测试；
- 音频在成功、失败、取消和超时四条路径均触发删除；
- 供应商密钥不出现在小程序包、日志和 Git；
- PII、逐字稿与完整提示词不会进入日志；
- 每个固定错误码映射正确提示与额度结果；
- 客户端断线恢复不会重复调用模型；
- `finalize` 重复调用返回同一结果，不重复收费。

#### AI 真机与服务验收

- 中国内地 Wi-Fi、5G 与较弱网络分别记录至少 30 次端到端耗时；
- 成功率、P50、P95、失败率和单张成本形成报告；
- 静音、环境人声、雨、街道、自然、音乐、突发声和持续低频全部覆盖；
- 结构化声音描述不显示逐字谈话内容；
- 预算耗尽、额度不足、供应商限流和超时都能回退本地版；
- 用户能明确指出点击哪个按钮才会上传并消耗额度；
- AI 分享图在目标手机全屏清晰，显式标识可见，隐式标识通过独立检查；
- Z-Image 与混元盲评至少使用相同的 24 组输入，每组覆盖三种 Mood；
- 只有完成隐私指引、服务协议、AI 标识材料和平台审核检查后才允许提审。

发布门禁：

- 免费版任何项目网络调用不为零；
- AI 点击前发生建号、上传或模型调用；
- 录音删除任一路径无法证明；
- 幂等或额度事务测试失败；
- P95 超过 15 秒且无可恢复交互；
- 单张成功成本超过 0.15 元且未复盘；
- AI 标识无法在最终分享文件中保留；
- 隐私说明与真实供应商保留条款不一致；
- 任一门禁未通过，均不得发布 AI 版。

### 19.24 后续实施里程碑

现有 Web `M0–M5` 和微信小程序 `W0–W5` 与以下 AI 里程碑是不同阶段。必须先验证并完成
当前小程序里程碑，不能把未提交的 W2 工作与 AI 改动混在同一提交。

#### AI-M0：规格冻结与无付费调用骨架（实现完成，默认关闭）

- 将本节拆成代码接口、错误码、配置和测试 fixture；
- 建立 feature flag，默认关闭 AI；
- 建立 provider interface 和纯本地 mock；
- 不调用真实供应商。

验收：现有免费流程不回归；mock 状态机、错误和幂等测试通过。

#### AI-M1：免费版声纹与八套配方（实现完成）

- 加入时间声纹；
- 移除时间戳主 seed；
- 实现八套本地配方；
- 完成本地包体、性能、离线与确定性测试。

验收：第 19.23 节免费版验收全部通过。

#### AI-M2：账户、额度与任务服务（本地 mock 实现完成）

- 微信无感登录；
- 账户、额度、流水、任务与预算；
- mock Captioner 和 mock 生图；
- 超时、取消、恢复、幂等与日志脱敏。

验收：不使用真实模型也能完成所有成功与失败流程；额度事务测试通过。

#### AI-M3：Qwen3-Omni Captioner（安全模拟完成，真实门禁待验证）

- 临时 PCM 上传；
- 结构化输出、过滤与人声保护；
- 全路径删除与 TTL；
- Captioner 成本、延迟和失败压测。

验收：只生成结构化描述，不调用图像模型；删除证据和隐私文案通过审查。

#### AI-M4：Z-Image-Turbo 与混合渲染（本地框架完成，真实门禁待验证）

- 确定性提示词模板；
- 864×1152 单图；
- 压缩、结果恢复、1080×1440 分享合成；
- 显式与隐式 AI 标识；
- 离线混元盲测。

验收：画质、延迟、成本、分享和标识门禁通过；决定正式图像供应商。

#### AI-M5：真机、审核与受控发布（仓库准备完成，外部门禁阻塞）

- 国内真实网络压测；
- iOS/Android 真机；
- 隐私、协议、AI 标识、备案和小程序提审材料；
- 服务端预算、告警、熔断、密钥轮换和删除巡检；
- feature flag 小范围开放与回滚。

验收：第 19.23 节所有发布门禁通过后，才允许管理员发布。

每个 `AI-Mx` 必须单独测试、单独提交。未通过当前里程碑，不得预先实现下一阶段。

---

## 20. 给 Codex 的最终执行指令

请把本文件视为项目需求与工程约束的唯一主文档。

执行顺序：

1. 检查当前仓库，不能覆盖已有工作；
2. 本地版 W0–W5 保留在微信小程序分支；当前 AI 工作位于独立 AI 分支，不得切回旧
   Web 分支或把默认关闭的 AI 改动混入本地版提审提交；
3. 通过 Git 提交、工作区文件、测试结果和真机记录判断当前里程碑，不只依据文档中的“完成”文字；
4. W2 麦克风实现已完成自动检查，Android 最新修复和 iPhone 真机仍待最终验收；
   按用户要求继续 W3 → W5 时必须保留这些门禁，不能写成已通过；
5. W5 与 AI-M0–AI-M5 的仓库实现、测试、材料和自动门禁均已完成；AI-M3/M4 只验证
   合成输入、注入式 mock transport、标识门禁与离线评测框架。`check:ai-release` 当前
   必须保持 `blocked-external-gates`，真实上传、供应商、用户入口和发布等待外部证据；
6. 每次只处理当前里程碑，每个里程碑先验证、再单独提交，不混入下一阶段；
7. 保持依赖最少，不进行无关扫描、扩展或重构；
8. 免费版本地流程的零网络、零账户、零 API 成本是硬边界；
9. AI 上传、建号和额度预占只能发生在用户点击明确的 AI 生成按钮之后；
10. 遇到 Windows 无法复现的真机问题，记录设备、系统、微信版本、步骤和实际结果，
    不得猜测已经通过；
11. 没有供应商凭证时使用 mock provider，不得声称真实 AI 链路已经验证；
12. 没有部署、备案、隐私或审核凭证时，只准备材料，不得声称已经上线；
13. 所有隐私、AI 标识、数据删除和额度声明必须与真实实现一致；
14. 完成每个阶段后更新简洁的 `FINAL_REPORT.md`，记录验证、遗留门禁和下一里程碑。

最终产品应当是一段完整、克制、可理解的体验，而不是技术演示仪表盘。

> 听十秒此刻的声音，选择一种心情，留下这一刻的颜色。
