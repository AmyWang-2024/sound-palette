# Sound Palette M4 人工测试清单

自动化测试不能替代真实移动设备的权限、系统分享和内置浏览器行为。以下项目在实际设备
完成前必须标记为“待验证”，不得声称已经通过。

## 测试前准备

麦克风只在 HTTPS 或本机 `localhost` 安全上下文可用。同一 Wi-Fi 下通过
`http://<电脑局域网 IP>:5173` 打开时，手机应进入示例模式，这不能用于验证手机麦克风。
真实麦克风测试需使用正式 HTTPS 地址。

每台设备先完成一次“先体验示例”，再在可用时完成一次真实麦克风流程。导出后检查 PNG
尺寸为 1080 × 1440，文字完整且没有裁切。

## Windows Chrome

1. 从首页完成示例、Mood 和结果页流程。
2. 保存 PNG，检查尺寸、作品、时间、Mood、三层比例和三个标签。
3. 点击分享；不支持文件分享时应自动下载，作品不能丢失。
4. 聆听期间切到后台，再回来确认捕获已安全结束。

## Android Chrome

1. 在 HTTPS 地址允许麦克风，确认 10 秒后权限指示停止。
2. 分别测试竖屏和横屏，确认按钮可点击、无横向溢出。
3. 保存 PNG，并用系统分享发送到一个测试目标。
4. 取消一次系统分享，确认仍出现可保存的 PNG 预览。
5. 开启系统“减少动态效果”（若设备提供），确认粒子和运动明显降低。

## iPhone Safari

1. 在 HTTPS 地址测试允许和拒绝麦克风两种路径。
2. 在竖屏、横屏及刘海/底部安全区检查页面无裁切。
3. 点击保存图片，确认出现 PNG 预览和“长按图片保存”提示。
4. 点击分享，确认系统分享面板包含 PNG；取消后作品仍保留。
5. 聆听时锁屏或切后台，再返回确认麦克风不再占用。

## iPad Safari

1. 重复 iPhone 的权限、保存与分享测试。
2. 分屏与横竖屏切换后确认画布重排正常。
3. 使用键盘完成首页、示例、Mood、换 Mood 和再听一次的基本操作。

## 微信内置浏览器

1. 麦克风不可用时，确认出现“可以先体验示例”以及 Safari/Chrome 重开提示。
2. 完成示例流程，确认结果页可用。
3. 保存或分享受限时，确认出现 PNG 预览、长按保存提示和外部浏览器提示。

## 声音场景

在麦克风正常的设备上依次测试安静房间、正常说话、低沉持续声、拍手或短促高频声、
环境背景声与拒绝权限。确认声音摘要和画面有明显区别，重复聆听不会持续占用麦克风。
