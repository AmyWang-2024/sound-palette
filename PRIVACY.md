# Sound Palette 隐私说明

## 当前 M4 实现

当前版本仅在用户点击“开始聆听”或“再听一次”后请求权限。浏览器通过 Web Audio API 在内存中
读取 10 秒时域与频域帧，并立即转为相对响度、低中高频能量和变化程度；结束或停止后释放
麦克风与 AudioContext。项目不会使用 `MediaRecorder`，不会创建音频 Blob 或音频文件，
不会保存或上传原始帧，也未接入统计、广告、追踪、账号、服务器或数据库。

示例模式只生成预设的数值特征曲线，不播放、下载或保存任何录音素材。

用户主动点击“保存图片”或“分享”时，浏览器会在内存中生成一张只含静态作品、声音摘要、
Mood、标签与时间的 PNG。PNG 不含原始音频；只有用户主动下载、长按保存或调用系统分享
后才会离开页面内存。

## MVP 隐私边界

后续麦克风功能只能在用户主动点击后启动，并只在设备内存中实时计算短期数值特征。
项目不得使用 `MediaRecorder`，不得生成音频文件或音频 Blob，不得将原始时域或频域帧
写入本地存储，也不得上传麦克风数据。会话仅允许保留声音摘要、Mood、视觉 seed 和用户
主动导出的图片。
