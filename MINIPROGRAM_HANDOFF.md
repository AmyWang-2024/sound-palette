# Sound Palette 微信小程序 v0.1 交接文件

本文件是微信小程序版本的需求与工程约束主文档。Web 版本仍只以 `CODEX_HANDOFF.md`
为主文档；小程序工作不得改变 Web MVP 的既有隐私事实或发布状态。

## 1. 已确认的产品决策

- 中国大陆个人主体，账号已注册并取得 AppID；
- 中文名称：`我的声音相册`；英文名称保持 `Sound Palette`；
- 服务类目：`工具 > 图片处理`；
- 小程序图标与简介已准备；
- 保留 Web 版，新增微信原生小程序，不使用 WebView、React、Taro、服务器或数据库；
- 复用 `packages/core` 中的纯 TypeScript 声音摘要、Mood、seed、标签与布局规则；
- 微信可能生成的临时录音文件允许存在到会话结束，但必须立即删除，绝不上传或持久化；
- AppID、AppSecret、管理员凭证不得提交 Git；
- 未完成备案、审核和管理员发布前，不得声称已经上架。

## 2. 账号与发布门禁

W0 的账号门禁已完成：

1. 已注册中国大陆个人主体小程序并取得 AppID；
2. 已确认中文名称“我的声音相册”可用；
3. 已选择与抽象图片生成工具真实匹配的 `工具 > 图片处理` 类目；
4. 小程序图标与简介已准备；
5. 后续发布前仍须完成小程序备案与用户隐私保护指引。

受版本控制的 `project.config.json` 使用 `touristappid` 占位；真实 AppID 只存放在被 Git
忽略的本机 `project.private.config.json`，不得提交。

## 3. 隐私边界

- 只有用户点击“开始聆听”后才请求隐私与录音授权；
- 只分析十秒实时 PCM 数值帧，不做语音识别；
- 不上传麦克风帧、摘要或临时音频；
- 不调用持久化音频保存；
- `RecorderManager` 停止后立即释放监听器，并删除返回的临时文件；
- 用户主动保存作品时才请求相册写入权限；
- 隐私指引必须如实披露麦克风、临时文件删除和相册写入。

## 4. 工程结构

- `packages/core`：Web 与小程序共享的纯逻辑，不引用 DOM、Web Audio、微信 API 或 PixiJS；
- `src`：Web 平台适配与兼容 re-export；
- `miniprogram`：WXML、WXSS、TypeScript 页面和微信平台适配；
- `project.config.json`：微信开发者工具项目配置，占位 AppID。

共享核心固定包含：

- `AudioFrame`、`SoundSummary`、`Mood`、`SoundVisualInput`；
- PCM/频域数值计算与十秒摘要；
- 固定 seed 布局、Mood 更新、稳定标签和示例数据。

## 5. 里程碑

### W0：账号与工程骨架

- 账号、AppID、名称和个人主体类目门禁完成；
- 建立本文件、`packages/core` 和原生 TypeScript 小程序骨架；
- Web 使用共享 core，现有测试与生产构建通过；
- 不接麦克风，不实现正式小程序流程。

### W1：原生示例流程

- 首页、十秒示例、Mood、结果四状态；
- 原生 Canvas 2D 固定构图与三种 Mood；
- 开发者工具和真机性能验证。

### W2：麦克风与隐私

- `RecorderManager`、16 kHz 单声道 PCM、2048 点窗口、`fft.js@4.0.4`；
- 权限异常、十秒摘要、后台停止、重复聆听；
- 停止后立即删除临时文件；
- iOS 与 Android 真机确认 PCM 格式、端序与清理行为。

### W3：导出与分享

- 1080×1440 PNG；
- 保存相册、图片分享、取消分享和失败兜底；
- 作品、时间、Mood、三层比例与三个标签完整。

### W4：真机与审核 QA

- 微信开发者工具、Android 微信、iPhone 微信；
- 权限、隐私、后台释放、横竖屏、低端机性能和包体；
- 体验版、审核截图、文案和功能路径。

### W5：备案、提审和发布

- 完成备案与隐私指引；
- 上传开发版本、设置体验版、提交审核；
- 审核通过后由管理员手动发布。

每个里程碑必须测试、构建、独立 commit 并推送后才能进入下一阶段。

## 6. 发布阻塞条件

- 个人主体没有真实匹配的服务类目；
- 任一目标平台不能可靠解析 PCM 实时帧；
- 临时音频文件不能确认删除；
- 隐私声明与实际 API 不一致；
- 真机持续占用麦克风；
- Web 回归测试或构建失败；
- 备案或审核未通过。
