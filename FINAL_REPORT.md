# Sound Palette 当前阶段报告

更新时间：2026-07-27

## 已完成并有提交证据

- Web M0–M5：保留为已完成基线。
- 微信小程序 W0：`ebb3065`。
- 微信小程序 W1：`45b0df7`。
- `CODEX_HANDOFF.md` 已统一 Web、小程序 W0–W5 和未来 AI-M0–AI-M5 的需求顺序；
  `MINIPROGRAM_HANDOFF.md` 仅保留历史跳转，不再是第二份主文档。

## 当前阶段

W2“麦克风与隐私”实现及自动检查已经完成。清理逻辑只把明确的文件不存在错误视为成功；
权限错误或未知状态会丢弃结果。声音响应已使用频段占比与非线性增强拉开视觉差异。
Android 最新构建和 iPhone 真机仍待最终验收，因此不能宣称 W2 真机通过。

W3“导出与分享”实现及自动检查已经完成：使用独立 Canvas 重绘 1080×1440 PNG，支持
用户主动保存相册、微信图片分享、取消和预览兜底；操作后释放画布并删除临时 PNG。
Android 和 iPhone 的像素、文字裁切、相册与分享菜单仍待最终验收。

W4 已加入低、中、高三档渲染限制，统一小程序导航名称，并准备真机、审核截图和功能路径
材料。微信开发者工具 W4 预览编译成功，包体 119,579 Byte；这不等同于体验版或真机通过。

W5 仓库准备已完成：提供发布检查命令、平台隐私字段、备案/体验版/提审/发布操作手册和
证据表。发布检查返回 `ready-for-manual-gates`，扫描的 miniprogram 目录为 130,210 Byte。
未上传开发版本、未设置体验版、未提审、未发布。

AI-M0 已在独立 `feat/sound-palette-ai-v0.1` 分支完成：AI feature flag 和真实供应商开关
默认关闭；已建立固定错误码、未来契约、本地 mock Captioner/生图 provider 和幂等状态机。
当前小程序入口、包体、网络和隐私行为没有改变，也没有真实供应商调用。

AI-M1 已完成实现与自动检查：实时帧在当前会话内压缩为 20 段归一化时间声纹，使用量化
哈希生成稳定 `local-v1` seed，不再使用时间戳作为主体构图 seed。八个固定 fixture 分别
稳定命中同心场、流动色带、粒子星座、层叠纸面、垂直雨线、径向脉冲、碎片网格和宁静
地平线；Mood 切换保留配方、seed、声纹和主体布局。原始帧在完成或退出后清空。

AI-M2 已完成本地 mock 实现与自动检查：最小微信账户映射、业务 token、额度账户与流水、
任务元数据、用户限速、项目日预算、原子预占/结算/释放、模拟音频接收、有限轮询、取消、
超时、断开恢复、再生成和幂等 finalize 均有测试。该实现不接收 PCM，不调用微信或模型，
不发起网络请求，不是已部署服务器，小程序 AI 入口仍默认关闭。

AI-M3 已完成安全模拟实现与自动检查：结构化 Caption 具有数量/长度校验、PII 与逐字内容
过滤、人声泛化、低置信度保守降级和一次受控修复；合成 PCM 只在内存中存在，并在成功、
失败、取消、超时和 TTL 路径留下删除证据。Qwen3-Omni 候选适配器默认关闭，仅使用注入
mock transport 验证。没有真实上传、供应商调用、保留条款、成本、延迟或云端删除证据。

AI-M4 已完成本地框架与自动检查：确定性提示词固定为 864×1152、8 步、单图和关闭扩写；
Z-Image 与混元候选共用默认关闭的注入式 transport；1080×1440 混合 Canvas 叠加本地
声纹并绘制“AI 生成”。mock 标识证据会被分享门禁拒绝。盲测框架包含 24 组 × 3 Mood ×
2 匿名候选的 144 个评分项，不自动选择供应商。没有真实生图、盲评或隐式标识验证。

AI-M5 已完成仓库发布准备：21 项证据门禁区分 automatic、real-device、platform、
supplier、operations 和 mock；错误证据类型与 mock 不能冒充通过。预算/并发、熔断、
密钥元数据轮换、五路径删除巡检、稳定灰度和一键回滚均有测试。隐私、条款、审核、运维、
真机网络矩阵和 JSON 证据表已准备。当前 kill switch 开启、灰度 0%，AI 入口及真实
provider 关闭。

## 本次验证

- 文档差异检查通过；
- 124 个标题无重复，46 个代码围栏成对；
- `npm run test`：23 个测试文件、117 项测试全部通过；
- `npm run build`：成功；
- `npm run build:miniprogram`：共享核心构建及小程序 TypeScript 检查成功。
- `npm run check:miniprogram-release`：`ready-for-manual-gates`，小程序目录
  148,756 Byte；仍低于 1.5MB 主包门禁。
- 微信开发者工具 AI-M1 预览编译成功，预览包 128,738 Byte；这不等于真机通过。
- 微信开发者工具 AI-M4 分支预览编译成功，预览包 128,763 Byte；AI 入口仍默认关闭，
  这不等于 AI 功能或真机通过。
- `npm run check:ai-release`：`blocked-external-gates`；4 项仓库自动证据通过，
  17 项真机、网络、平台、供应商和生产运维门禁未验证，0 项错误。

## 遗留门禁

- Android 必须对最新清理和视觉响应修复重新执行完整 W2 真机测试；
- iPhone 尚无真机验证记录；
- 不同声音必须产生明显数据和画面差异；
- 临时录音文件必须确认删除，不能持续占用麦克风；
- W3 导出图片尺寸、文字裁切、相册权限、分享取消和临时 PNG 清理需要双平台验证；
- AI-M1 的五类声音差异、八套配方品牌一致性、飞行模式、连续十次生成和中低端机性能
  仍需按 `docs/ai/AI_M1_MANUAL_TEST.md` 在 Android 与 iPhone 留下完整证据；
- AI-M2 目前只有内存 mock；没有服务器部署、微信真实登录、持久化数据库或真实业务
  网络验证，不能宣称账户、额度或任务服务已经上线；
- AI-M3 的 PCM 与 Caption 测试全部为合成输入；Qwen 国内地域、正式能力、真实 P50/P95、
  单次成本、供应商保留条款和服务端删除证据均未验证；
- AI-M4 没有真实候选图片、150–350KB 压缩样本、盲评结果、正式供应商结论或真实
  finalize 隐式标识证据；mock 分享证据按设计不能通过发布门禁；
- AI-M5 未部署服务、未上传 AI 体验版、未提审、未发布；Android/iPhone AI 真机、
  国内网络 30 次样本、平台隐私、条款、备案、供应商合同、生产删除/预算/标识证据、
  盲评、审核和管理员发布共 17 项仍阻塞；
- 提审所需的真机、隐私、备案、供应商、删除与标识前置证据完成前不得提审；审核通过和
  管理员发布前不得开放灰度、声称已发布或声称双平台 AI 通过。
